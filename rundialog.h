#ifndef NBRUNDIALOG_H
#define NBRUNDIALOG_H

#include <QDialog>

class NBRunDialog : public QDialog
{
    Q_OBJECT
public:
    explicit NBRunDialog(QWidget *parent = 0);
    ~NBRunDialog();

signals:

public slots:
};

#endif // NBRUNDIALOG_H
